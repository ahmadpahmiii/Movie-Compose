package com.example.presentation.designsystem

import androidx.compose.foundation.shape.RoundedCornerShape

object AppShape {
    val Small = RoundedCornerShape(AppSpacing.XS)
    val Medium = RoundedCornerShape(AppSpacing.S)
    val Large = RoundedCornerShape(AppSpacing.M)
    val ExtraLarge = RoundedCornerShape(AppSpacing.L)
    val Pill = RoundedCornerShape(AppSpacing.XXL)
}
